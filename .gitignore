target/
dist/
node_modules/
*.log
# Peer configuration is a WireGuard private key. It belongs in the OS keychain,
# never on disk and never here.
*.peer.conf
wg-*.conf

# Generated by tauri-build on every compile; regenerated from tauri.conf.json
# and the plugin set, so committing it only creates noisy diffs.
/src-tauri/gen/schemas/
